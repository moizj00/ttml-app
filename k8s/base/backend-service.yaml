apiVersion: v1
kind: Service
metadata:
  name: ttml-backend
  namespace: ttml
  labels:
    app: ttml
    component: backend
spec:
  selector:
    app: ttml
    component: backend
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  type: ClusterIP
