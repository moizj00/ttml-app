[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Increase start timeout to allow migrations to complete before health check.
# Default is 60s; migrations on first deploy can take 30-60s.
healthcheckTimeout = 120
