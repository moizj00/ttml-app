# Enforce pnpm as the only package manager for this repo.
# Running `npm install` or `yarn` will fail with a clear error message.
# Use `pnpm install` instead.
engine-strict=true
package-manager-strict=true
ed (see commit ab9af